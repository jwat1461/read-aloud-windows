# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Generated audio
*.wav

# Editors
.vscode/
.idea/
*.swp

# Windows
Thumbs.db
desktop.ini

# Chrome extension build output
extension.crx

# Signing keys. The .pem owns the extension ID — anyone holding it can ship
# updates as you — so it never goes in a commit, public repo or not.
extension.pem
*.pem
